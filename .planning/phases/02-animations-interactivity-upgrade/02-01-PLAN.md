---
phase: 02-animations-interactivity-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Reveal.tsx
  - app/about/page.tsx
  - app/approach/page.tsx
  - app/pathways/page.tsx
  - app/not-found.tsx
  - components/ArticleLayout.tsx
  - components/BiggerPicture.tsx
  - components/ContactInquiry.tsx
  - components/Hero.tsx
  - components/InsightsBento.tsx
  - components/SiteFooter.tsx
  - components/StrategicPathways.tsx
  - components/TeamPortraits.tsx
  - components/TestimonialBlock.tsx
  - components/ThreeStep.tsx
autonomous: true
requirements:
  - T1-animations
  - T1-performance
---

<objective>
Audit and tune `Reveal` and `RevealWords` usage across the site. Preserve the fixed hero hydration behavior, keep reduced-motion support, and make reveal timing consistent enough that later animation lanes can build on it.

Working lane: `anim/reveal-audit`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-animations-interactivity-upgrade/02-CONTEXT.md
@.planning/phases/02-animations-interactivity-upgrade/02-RESEARCH.md
@.planning/phases/02-animations-interactivity-upgrade/02-UI-SPEC.md
@project-notes/DESIGN_GUIDE.md
@project-notes/COPY_VOICE_GUIDE.md
</context>

<threat_model>
No external input, secrets, or server APIs are touched. Main risks are frontend regressions: hydration mismatch, delayed LCP, unreadable entry animation, and reduced-motion violations. Block any change that alters lead forms, compliance copy, API routes, or environment handling.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create isolated reveal audit lane</name>
  <files>git worktree or branch only</files>
  <action>
Use an isolated lane before editing. Preferred: create a sibling worktree named `../kismet-anim-reveal-audit` on branch `anim/reveal-audit`. If a worktree is not practical in the current environment, create or switch to branch `anim/reveal-audit` and confirm the main working tree changes are not mixed into this lane.
  </action>
  <verify>
Run `git status --short` and confirm only this lane's intended files are changed before implementation begins.
  </verify>
</task>

<task type="auto">
  <name>Task 2: Inventory reveal usage</name>
  <files>components/Reveal.tsx and all import sites</files>
  <action>
Search for all `Reveal` and `RevealWords` imports and call sites. Categorize each usage as hero, section heading, body block, repeated card, CTA, or article content. Note any inconsistent delays, y offsets, or `immediate` usage.

Do not change code during the inventory except for a small inline comment only if it prevents a future executor mistake.
  </action>
  <verify>
The summary lists every file using `Reveal` or `RevealWords` and identifies any call sites intentionally left unchanged.
  </verify>
</task>

<task type="auto">
  <name>Task 3: Tune central reveal behavior only where justified</name>
  <files>components/Reveal.tsx</files>
  <action>
If the audit shows the central defaults are inconsistent with the design guide, tune only `Reveal.tsx`. Keep the same public props. Do not remove `immediate`. Do not change the rendered tag strategy that keeps SSR and hydration aligned.

Prefer conservative values matching the design guide: subtle y movement, calm cinema ease, one-time viewport reveal, and reduced-motion instant rendering.
  </action>
  <verify>
Run `npm run type-check`. Inspect home and at least one article page to confirm hero headline still appears promptly and non-hero reveals still trigger on scroll.
  </verify>
</task>

<task type="auto">
  <name>Task 4: Normalize obvious call-site timing outliers</name>
  <files>Reveal import sites only</files>
  <action>
Adjust only call sites that clearly break the intended sequence. Keep edits surgical. Do not redesign sections, rewrite copy, or introduce new wrappers.

Do not make card-specific animation decisions here. Repeated card surfaces belong to Plan 02.
  </action>
  <verify>
Run `npm run type-check` and visually review home, approach, pathways, insights, about, contact, and one article page.
  </verify>
</task>

</tasks>

<verification>
- `npm run type-check`
- `npm run lint`
- Browser review at 375px and 1440px for the key pages listed in `02-UI-SPEC.md`
- Reduced-motion emulation confirms reveal movement is disabled or effectively instant
- Search confirms no em-dashes or en-dashes were introduced
</verification>

<success_criteria>
- All `Reveal` and `RevealWords` usages are inventoried
- Hero `immediate` behavior is preserved
- Central reveal props remain backwards compatible
- No card-specific polish is implemented in this plan
- No GSAP work is added
- Summary includes changed files, unchanged files, verification results, and any follow-up notes for Plan 02
</success_criteria>

<output>
Create `.planning/phases/02-animations-interactivity-upgrade/02-01-SUMMARY.md` after completion.
</output>
