---
phase: 02-animations-interactivity-upgrade
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - components/PageTransition.tsx
  - app/layout.tsx
  - app/globals.css
autonomous: true
requirements:
  - T1-animations
  - T1-smooth-transitions
  - T1-performance
---

<objective>
Refine page-level transitions after component-level motion has settled. Keep the transition calm, readable, reduced-motion safe, and compatible with the App Router layout.

Working lane: `anim/page-transitions`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-animations-interactivity-upgrade/02-CONTEXT.md
@.planning/phases/02-animations-interactivity-upgrade/02-RESEARCH.md
@.planning/phases/02-animations-interactivity-upgrade/02-UI-SPEC.md
@.planning/phases/02-animations-interactivity-upgrade/02-01-SUMMARY.md
@.planning/phases/02-animations-interactivity-upgrade/02-02-SUMMARY.md
@.planning/phases/02-animations-interactivity-upgrade/02-03-SUMMARY.md
@project-notes/DESIGN_GUIDE.md
</context>

<threat_model>
Page transitions wrap every page. Main risks are blank frames, overlapping pages, scroll position surprises, hydration issues, and reduced-motion violations. Do not alter API routes, metadata, schema data, header/footer content, or compliance text.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create isolated page transition lane</name>
  <files>git worktree or branch only</files>
  <action>
Use branch or worktree `anim/page-transitions`. Start only after Plans 01, 02, and 03 are summarized or explicitly marked as not merged.
  </action>
  <verify>
Run `git status --short` and confirm the lane starts clean.
  </verify>
</task>

<task type="auto">
  <name>Task 2: Audit current route transition behavior</name>
  <files>components/PageTransition.tsx, app/layout.tsx</files>
  <action>
Review how `PageTransition` is mounted in `app/layout.tsx`. Click between all top-level routes and one article route. Note any blank flash, double animation, header/footer mismatch, scroll issue, or motion that feels too heavy.
  </action>
  <verify>
The summary includes route pairs tested and observed issues before changes.
  </verify>
</task>

<task type="auto">
  <name>Task 3: Tune PageTransition conservatively</name>
  <files>components/PageTransition.tsx</files>
  <action>
If tuning is needed, adjust duration, y, opacity, scale, or blur only. Keep `AnimatePresence`, pathname keying, and reduced-motion behavior. Avoid overlay wipes, masks, loaders, or GSAP for this plan.
  </action>
  <verify>
Run `npm run type-check`. Navigate between all key routes and confirm there is no blank or overlapping content.
  </verify>
</task>

<task type="auto">
  <name>Task 4: Confirm global CSS view-transition rules do not conflict</name>
  <files>app/globals.css</files>
  <action>
Review the existing View Transition API CSS rules. If they conflict with Motion page transitions, disable or scope only the conflicting rule. Do not remove unrelated styles.
  </action>
  <verify>
Browser navigation still feels like one transition system, not two stacked systems.
  </verify>
</task>

</tasks>

<verification>
- `npm run type-check`
- `npm run lint`
- `npm run build`
- Browser route navigation review across key pages
- Reduced-motion route navigation review
- Search confirms no em-dashes or en-dashes were introduced
</verification>

<success_criteria>
- Route transitions are calm and do not create blank frames
- Reduced motion renders pages without movement
- Header and footer remain stable
- No new GSAP work is introduced
- Summary includes before and after route observations
</success_criteria>

<output>
Create `.planning/phases/02-animations-interactivity-upgrade/02-04-SUMMARY.md` after completion.
</output>
