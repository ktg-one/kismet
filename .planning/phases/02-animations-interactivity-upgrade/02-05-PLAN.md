---
phase: 02-animations-interactivity-upgrade
plan: 05
type: execute
wave: 3
depends_on:
  - 02-04
files_modified:
  - components/Hero.tsx
  - components/SmoothScroll.tsx
  - app/globals.css
autonomous: true
requirements:
  - T1-animations
  - T1-performance
---

<objective>
Perform the final GSAP pass after the safer Motion and CSS lanes are complete. Audit existing GSAP and Lenis behavior first. Add new GSAP only if it solves a clear scroll or timeline problem that Motion or CSS cannot handle cleanly.

Working lane: `anim/gsap-final-pass`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-animations-interactivity-upgrade/02-CONTEXT.md
@.planning/phases/02-animations-interactivity-upgrade/02-RESEARCH.md
@.planning/phases/02-animations-interactivity-upgrade/02-UI-SPEC.md
@.planning/phases/02-animations-interactivity-upgrade/02-04-SUMMARY.md
@project-notes/DESIGN_GUIDE.md
@C:/Users/kevin/.agents/skills/awwwards-animations/references/gsap-react.md
@C:/Users/kevin/.agents/skills/awwwards-animations/references/performance.md
</context>

<threat_model>
GSAP and Lenis affect scroll behavior across the site. Main risks are memory leaks, stale ScrollTriggers after route changes, mobile scroll jank, broken reduced-motion behavior, and duplicate ticker callbacks. Do not change business logic, forms, API routes, metadata, or compliance content.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create isolated GSAP lane</name>
  <files>git worktree or branch only</files>
  <action>
Use branch or worktree `anim/gsap-final-pass`. Do not start until Plans 01 through 04 are complete or intentionally skipped.
  </action>
  <verify>
Run `git status --short` and confirm the lane starts clean.
  </verify>
</task>

<task type="auto">
  <name>Task 2: Audit current GSAP and Lenis integration</name>
  <files>components/Hero.tsx, components/SmoothScroll.tsx</files>
  <action>
Review `useGSAP` scoping, plugin registration, ScrollTrigger setup, ticker integration, reduced-motion behavior, and cleanup. Confirm the hero watermark and background parallax behave correctly on home and any page using `Hero`.
  </action>
  <verify>
The summary states whether current GSAP behavior is accepted as-is or needs a fix.
  </verify>
</task>

<task type="auto">
  <name>Task 3: Fix only proven GSAP issues</name>
  <files>components/Hero.tsx, components/SmoothScroll.tsx, app/globals.css</files>
  <action>
If the audit finds a concrete issue, fix it surgically. Acceptable fixes include scoping selectors, cleanup, ScrollTrigger refresh behavior, reduced-motion guards, or removing stale CSS that fights GSAP.

Do not add new scroll scenes, pinned sections, custom cursors, SplitText, or decorative timeline effects unless the summary proves a specific need and the change remains small.
  </action>
  <verify>
Run `npm run type-check`, `npm run lint`, and `npm run build`. Review scroll behavior on mobile and desktop.
  </verify>
</task>

<task type="auto">
  <name>Task 4: Final animation integration pass</name>
  <files>Phase 2 changed files only</files>
  <action>
Review the combined results from all Phase 2 lanes. If there are timing conflicts between reveal, cards, CTAs, page transitions, and GSAP, resolve the smallest conflict only. Do not broaden the phase.
  </action>
  <verify>
Run final browser review across all key pages in `02-UI-SPEC.md`.
  </verify>
</task>

</tasks>

<verification>
- `npm run type-check`
- `npm run lint`
- `npm run build`
- Desktop and mobile scroll review
- Reduced-motion review
- Confirm ScrollTrigger markers are not present
- Search confirms no em-dashes or en-dashes were introduced
</verification>

<success_criteria>
- Existing GSAP behavior is audited and documented
- Any GSAP changes are limited to proven issues
- No new GSAP spectacle is added
- Lenis and ScrollTrigger remain stable after route navigation
- Final Phase 2 animation pass is ready for verification
</success_criteria>

<output>
Create `.planning/phases/02-animations-interactivity-upgrade/02-05-SUMMARY.md` after completion.
</output>
