---
phase: 02-animations-interactivity-upgrade
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - components/InsightsBento.tsx
  - components/StrategicPathways.tsx
  - components/ThreeStep.tsx
  - components/TeamPortraits.tsx
  - app/about/page.tsx
  - app/pathways/page.tsx
autonomous: true
requirements:
  - T1-animations
  - T1-performance
---

<objective>
Polish card hover and reveal behavior as its own animation lane. Keep card motion subtle, consistent, and CSS-first unless a component clearly needs Motion.

Working lane: `anim/card-polish`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-animations-interactivity-upgrade/02-CONTEXT.md
@.planning/phases/02-animations-interactivity-upgrade/02-RESEARCH.md
@.planning/phases/02-animations-interactivity-upgrade/02-UI-SPEC.md
@project-notes/DESIGN_GUIDE.md
</context>

<threat_model>
No data or server behavior is touched. Main risks are visual inconsistency, hover-only meaning, mobile layout shift, and animation jank across repeated cards. Block any change that alters card copy, links, routes, forms, or compliance text.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create isolated card lane</name>
  <files>git worktree or branch only</files>
  <action>
Use branch or worktree `anim/card-polish`. Keep this lane independent from reveal, CTA, page transition, and GSAP lanes.
  </action>
  <verify>
Run `git status --short` and confirm this lane starts clean except for known planning artifacts.
  </verify>
</task>

<task type="auto">
  <name>Task 2: Audit card classes and surfaces</name>
  <files>app/globals.css and repeated card components</files>
  <action>
List all card-like surfaces that use `.glass-card`, `.kismet-surface`, `.card-lift-hover`, inline hover transforms, or grouped hover effects. Identify duplicates that can be normalized through existing utilities.
  </action>
  <verify>
The summary names every surface reviewed and states whether it changed or remained unchanged.
  </verify>
</task>

<task type="auto">
  <name>Task 3: Normalize CSS-first card motion</name>
  <files>app/globals.css and card components only</files>
  <action>
Prefer existing CSS utilities. If a card has bespoke hover movement that should match the shared vocabulary, switch it to `.card-lift-hover`, `.glass-card`, or `.kismet-surface` rather than creating a new abstraction.

Keep hover lift subtle. Avoid rotate, tilt, scale-heavy, cursor-tracking, or 3D card effects.
  </action>
  <verify>
Run `npm run type-check` and inspect cards on home, pathways, insights, about, and approach.
  </verify>
</task>

<task type="auto">
  <name>Task 4: Confirm mobile and reduced-motion behavior</name>
  <files>app/globals.css</files>
  <action>
Ensure card hover transforms are disabled or harmless under `prefers-reduced-motion`. Confirm mobile cards do not depend on hover for meaning.
  </action>
  <verify>
Browser review at 375px and desktop. Reduced-motion emulation shows no card movement.
  </verify>
</task>

</tasks>

<verification>
- `npm run type-check`
- `npm run lint`
- Browser review of repeated cards at 375px and 1440px
- Reduced-motion check
- Search confirms no em-dashes or en-dashes were introduced
</verification>

<success_criteria>
- Card motion is consistent across repeated surfaces
- No new animation dependency is added
- No page transition, CTA spring, or GSAP behavior is changed
- No layout shift is introduced
- Summary includes the final list of card surfaces changed and left alone
</success_criteria>

<output>
Create `.planning/phases/02-animations-interactivity-upgrade/02-02-SUMMARY.md` after completion.
</output>
