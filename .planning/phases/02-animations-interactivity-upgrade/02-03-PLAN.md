---
phase: 02-animations-interactivity-upgrade
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/MagneticCTA.tsx
  - components/SiteHeader.tsx
  - components/SiteFooter.tsx
  - components/Hero.tsx
  - components/ContactInquiry.tsx
  - components/ArticleLayout.tsx
  - app/approach/page.tsx
autonomous: true
requirements:
  - T1-animations
  - T1-performance
---

<objective>
Refine the existing magnetic CTA effect so it is consistent, restrained, accessible, and safe on mobile. Do not redesign CTAs.

Working lane: `anim/magnetic-ctas`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-animations-interactivity-upgrade/02-CONTEXT.md
@.planning/phases/02-animations-interactivity-upgrade/02-RESEARCH.md
@.planning/phases/02-animations-interactivity-upgrade/02-UI-SPEC.md
@project-notes/DESIGN_GUIDE.md
</context>

<threat_model>
The CTA points users toward contact and booking flows, so link hrefs, labels, focus behavior, and tap targets must not regress. Main risks are pointer-only interaction, mobile weirdness, and motion that feels gimmicky.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create isolated CTA lane</name>
  <files>git worktree or branch only</files>
  <action>
Use branch or worktree `anim/magnetic-ctas`. Keep this lane scoped to CTA motion and direct call sites.
  </action>
  <verify>
Run `git status --short` before implementation.
  </verify>
</task>

<task type="auto">
  <name>Task 2: Audit MagneticCTA call sites</name>
  <files>components/MagneticCTA.tsx and import sites</files>
  <action>
Inventory every `MagneticCTA` call site. Confirm hrefs, labels, class names, and mobile contexts. Note any call site where width or layout makes magnetic movement risky.
  </action>
  <verify>
The summary lists all call sites and any intentionally unchanged ones.
  </verify>
</task>

<task type="auto">
  <name>Task 3: Refine motion boundaries</name>
  <files>components/MagneticCTA.tsx</files>
  <action>
Tune only the existing Motion implementation unless the audit proves a call-site fix is needed. Keep the maximum pull subtle. Preserve the public component props. Ensure reduced motion returns to a static transform. Avoid custom cursor behavior.
  </action>
  <verify>
Run `npm run type-check`. Confirm CTA hover still works on desktop and tap behavior is normal on mobile.
  </verify>
</task>

<task type="auto">
  <name>Task 4: Verify focus and keyboard behavior</name>
  <files>components/MagneticCTA.tsx and CTA call sites</files>
  <action>
Keyboard focus must remain visible and stable. If the wrapper or transform interferes with focus ring placement, adjust only the smallest necessary class or wrapper behavior.
  </action>
  <verify>
Tab through header, hero, footer, article CTA, approach CTA, and contact CTAs.
  </verify>
</task>

</tasks>

<verification>
- `npm run type-check`
- `npm run lint`
- Keyboard tab review of CTA locations
- Mobile tap review at 375px
- Reduced-motion check
- Search confirms no em-dashes or en-dashes were introduced
</verification>

<success_criteria>
- MagneticCTA remains API-compatible
- CTA hrefs and labels are unchanged unless a bug is found and documented
- Motion is restrained and disabled for reduced motion
- No card, page transition, or GSAP changes are made
- Summary includes CTA call-site inventory and verification notes
</success_criteria>

<output>
Create `.planning/phases/02-animations-interactivity-upgrade/02-03-SUMMARY.md` after completion.
</output>
