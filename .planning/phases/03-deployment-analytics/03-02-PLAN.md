---
phase: 03-deployment-analytics
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: ["app/layout.tsx", "package.json"]
autonomous: true
requirements: [T2. Deployment]
user_setup:
  - service: google-analytics
    why: "GA4 Tracking"
    env_vars:
      - name: NEXT_PUBLIC_GA_ID
        source: "Google Analytics Dashboard -> Data Streams -> Measurement ID"

must_haves:
  truths:
    - "Google Analytics 4 is only active in production environment"
    - "Tracking ID is sourced from NEXT_PUBLIC_GA_ID environment variable"
  artifacts:
    - path: "app/layout.tsx"
      provides: "GA4 component integration"
  key_links:
    - from: "app/layout.tsx"
      to: "Google Analytics"
      via: "next/third-parties/google"
---

<objective>
Implement Google Analytics 4 (GA4) tracking using @next/third-parties, with environment gating to ensure tracking only occurs in production.

Purpose: Gather site analytics without polluting data with development/preview traffic.
Output: GA4 integrated into the root layout.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/layout.tsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Dependencies</name>
  <files>package.json</files>
  <action>
    Install `@next/third-parties` to provide the official Next.js Google Analytics component.
  </action>
  <verify>
    <automated>npm list @next/third-parties</automated>
  </verify>
  <done>@next/third-parties added to package.json.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate GA4 with Environment Gating</name>
  <files>app/layout.tsx</files>
  <behavior>
    - Import GoogleAnalytics from @next/third-parties/google
    - Render GoogleAnalytics component in RootLayout
    - Only render if process.env.NODE_ENV === 'production' and NEXT_PUBLIC_GA_ID is present
  </behavior>
  <action>
    Modify `app/layout.tsx` to include the `GoogleAnalytics` component.
    Use the `NEXT_PUBLIC_GA_ID` environment variable.
    Wrap it in a conditional check for `process.env.NODE_ENV === 'production'`.
  </action>
  <verify>
    <automated>grep -q "GoogleAnalytics" app/layout.tsx</automated>
  </verify>
  <done>GA4 component integrated and gated by environment.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Environment Gating</name>
  <files>app/layout.tsx</files>
  <action>
    Confirm that the GA4 script is NOT rendered in the local development build by checking the output source or running a build-time check.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const content = fs.readFileSync('app/layout.tsx', 'utf8'); if (!content.includes('process.env.NODE_ENV === \'production\'')) throw new Error('Missing production check')"</automated>
  </verify>
  <done>Gating logic verified in source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Google | GA4 sends tracking data to Google |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Information Disclosure | Analytics | mitigate | Use official component, limit PII collection, follow privacy guidelines |
</threat_model>

<verification>
Run `npm run build` and ensure no LCP regressions or hydration errors introduced by GA4 script.
</verification>

<success_criteria>
GA4 tracking is active in production and absent in dev/preview environments.
</success_criteria>

<output>
Create `.planning/phases/03-deployment-analytics/03-02-SUMMARY.md` when done
</output>
