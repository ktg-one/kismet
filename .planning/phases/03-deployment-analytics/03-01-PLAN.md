---
phase: 03-deployment-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [T2. Deployment]
user_setup:
  - service: vercel
    why: "Environment variable configuration"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard"
      - name: GOOGLE_SERVICE_ACCOUNT_EMAIL
        source: "Google Cloud Console -> Service Accounts"
      - name: GOOGLE_SERVICE_ACCOUNT_KEY
        source: "Google Cloud Console -> Service Accounts -> Keys (JSON, flattened to one line)"
      - name: GOOGLE_SHEETS_ID
        source: "Google Sheets URL"
      - name: LEAD_INBOX_TO
        source: "Target email for leads"
      - name: LEAD_INBOX_FROM
        source: "Verified Resend sender email"
      - name: NEXT_PUBLIC_BOOKING_URL
        source: "Calendly or other booking link"

must_haves:
  truths:
    - "Vercel environment contains all required keys from lib/env.ts"
    - "Production leads successfully route to Resend and Google Sheets"
  artifacts: []
  key_links:
    - from: "Vercel Env"
      to: "lib/env.ts"
      via: "Runtime environment"
      pattern: "RESEND_API_KEY"
---

<objective>
Configure production environment variables in Vercel to enable the lead generation pipeline (Email + Sheets) and booking integration.

Purpose: Transition from local/preview testing to production-ready integrations.
Output: Vercel project configured with validated secrets.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/env.ts
@scripts/smoke-lead.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync Environment Variables to Vercel</name>
  <files></files>
  <action>
    Use the Vercel CLI to add the required environment variables to the production environment.
    Reference `lib/env.ts` for the full list of required keys.
    Ensure `GOOGLE_SERVICE_ACCOUNT_KEY` is provided as a single-line string with literal `\n` sequences (not actual newlines).
    
    Variables to set:
    - RESEND_API_KEY
    - LEAD_INBOX_TO
    - LEAD_INBOX_FROM
    - GOOGLE_SHEETS_ID
    - GOOGLE_SERVICE_ACCOUNT_EMAIL
    - GOOGLE_SERVICE_ACCOUNT_KEY
    - NEXT_PUBLIC_BOOKING_URL
  </action>
  <verify>
    <automated>vercel env ls production</automated>
  </verify>
  <done>All keys visible in Vercel production environment list.</done>
</task>

<task type="auto">
  <name>Task 2: Production Lead Pipeline Smoke Test</name>
  <files></files>
  <action>
    Execute the lead pipeline smoke test against the production deployment URL.
    The script `scripts/smoke-lead.mjs` triggers a test lead.
    Pass the production URL as the target.
  </action>
  <verify>
    <automated>node scripts/smoke-lead.mjs [PROD_URL]</automated>
  </verify>
  <done>Smoke test returns 200 and confirms lead delivery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel → External APIs | Secrets must be handled securely in Vercel Env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Vercel Env | mitigate | Use Vercel's encrypted environment variables; restrict access |
| T-03-SC | Tampering | vercel cli | mitigate | slopcheck + blocking human checkpoint for [ASSUMED] |
</threat_model>

<verification>
Confirm Vercel dashboard shows all variables in 'Production' scope.
</verification>

<success_criteria>
Production lead pipeline is fully functional and verified via smoke test.
</success_criteria>

<output>
Create `.planning/phases/03-deployment-analytics/03-01-SUMMARY.md` when done
</output>
